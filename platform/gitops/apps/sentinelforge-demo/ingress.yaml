apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sentinelforge-demo
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sentinelforge-demo
                port:
                  number: 80